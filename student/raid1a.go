package student

//Raid1a is drawing square area
func Raid1a(x, y int) {
	// matrix := [x][y]string{}

	// fmt.Println(matrix[0[0]])
	// for c := 0; c < x; c++ {
	// 	for r := 0; r < y; r++ {
	// 		if c == 0 && r == 0 || c == x - 1 && r ==
	// 	}
	// }
}
